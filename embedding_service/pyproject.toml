[project]
name = "embedding-service"
version = "0.1.0"
description = "Standalone embedding generation service (SentenceTransformer all-MiniLM-L6-v2)"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi>=0.138.0",
    "sentence-transformers>=5.6.0",
    "uvicorn>=0.49.0",
]
