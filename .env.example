QDRANT_URL=
QDRANT_API_KEY=

GROQ_API_KEY=
GROQ_MODEL=

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Set to "true" to enable the legacy /bookmark and /search endpoints, which
# load SentenceTransformer/torch on first use. Leave unset/"false" on
# memory-constrained deployments (e.g. Render free tier's 512MB limit).
EMBEDDINGS_ENABLED=false
