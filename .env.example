QDRANT_URL=
QDRANT_API_KEY=

GROQ_API_KEY=
GROQ_MODEL=

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Set to "true" to enable the legacy /bookmark and /search endpoints, which
# call out to the Hugging Face Inference API on first use. Leave unset/"false"
# on deployments where embeddings are not needed.
EMBEDDINGS_ENABLED=false

# Hugging Face API token used for embedding generation (app/embedding.py).
# Create one at https://huggingface.co/settings/tokens
HF_TOKEN=
